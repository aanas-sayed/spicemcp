# Pull simulator images onto the host:
#   docker compose pull --profile simulators   (Linux / Windows / Intel Mac)
#   docker compose pull --profile macos        (Apple Silicon Mac)

services:
  ltspice:
    image: aanas-sayed/docker-ltspice:latest
    profiles: [simulators]
    entrypoint: ["true"]

  ltspice-macos:
    image: aanas-sayed/docker-ltspice:macos-latest
    profiles: [macos]
    entrypoint: ["true"]

  ngspice:
    image: aanas-sayed/docker-ngspice:latest
    profiles: [simulators]
    entrypoint: ["true"]
